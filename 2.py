# Nested tuple
nested_tuple = ((3, 2), (1, 5), (2, 4))

# Sorting the nested tuple
sorted_tuple = tuple(sorted(nested_tuple))

print("Original Tuple:", nested_tuple)
print("Sorted Tuple:", sorted_tuple)